import { nextRoutes } from "@funstack/static/fs-routes";

// The built-in Next.js-like convention: `page.tsx` / `layout.tsx` files mapped
// to routes. Pass it explicitly so the convention is visible in the config.
export default nextRoutes();
